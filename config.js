// Shared between background.js and popup.js (loaded as plain scripts, no bundler).
const EXTPAY_EXTENSION_ID = "youtube-music-shortcuts"; // must match the slug registered on extensionpay.com
const TRIAL_DAYS = 3;
